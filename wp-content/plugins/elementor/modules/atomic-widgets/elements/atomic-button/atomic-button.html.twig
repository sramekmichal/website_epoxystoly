{% if settings.text is not empty %}
    {% set classes = settings.classes | merge( [ base_styles.base ] ) | join(' ') %}
	{% set id_attribute = settings._cssid is not empty ? 'id=' ~ settings._cssid | e('html_attr') : '' %}
    {% if settings.link.href %}
        <a
                href="https://sramekmichal.github.io/website_epoxystoly/wp-content/plugins/elementor/modules/atomic-widgets/elements/atomic-button/%20settings.link.href%20"
                target="{{ settings.link.target }}"
                class="{{ classes }}"
				{{ id_attribute }}
        >
            {{ settings.text }}
        </a>
    {% else %}
        <button class="{{ classes }}" {{ id_attribute }}>
            {{ settings.text }}
        </button>
    {% endif %}
{% endif %}
