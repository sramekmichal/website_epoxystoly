{% if settings.title is not empty %}
	{% set id_attribute = settings._cssid is not empty ? 'id=' ~ settings._cssid | e('html_attr') : '' %}
	<{{ settings.tag | e('html_tag') }} class="{{ settings.classes | merge( [ base_styles.base ] ) | join(' ') }}" {{ id_attribute }}>
	{% if settings.link.href %}
		<a href="/website_epoxystoly/wp-content/plugins/elementor/modules/atomic-widgets/elements/atomic-heading/%20settings.link.href%20" target="{{ settings.link.target }}" class="{{ base_styles['link-base'] }}">
			{{ settings.title }}
		</a>
	{% else %}
		{{ settings.title }}
	{% endif %}
	</{{ settings.tag | e('html_tag') }}>
{% endif %}
